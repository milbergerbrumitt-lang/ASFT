# Install a CUDA-compatible PyTorch build before this file, for example:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Core training stack
transformers>=4.49.0,<4.58.0,!=4.52.0,!=4.57.0
accelerate>=1.3.0,<1.12.0
peft>=0.17.0,<0.18.0
datasets>=2.16.0,<5.0.0
trl>=0.8.6,<0.10.0

# Tokenization and model files
sentencepiece>=0.2.0
tiktoken>=0.7.0
safetensors>=0.4.3
tokenizers>=0.20.0
protobuf>=4.25.0

# Runtime utilities
fire>=0.6.0
tqdm>=4.66.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pyyaml>=6.0.0
packaging>=23.0
python-dateutil>=2.8.2
typing_extensions>=4.8.0

# Math evaluation helpers used by eval/math_evaluation.
# vLLM and torch are intentionally not pinned here because they are CUDA-stack dependent.
sympy>=1.13.1
antlr4-python3-runtime==4.7.2
latex2sympy2>=1.9.1
word2number>=1.1
Pebble>=5.0.0
timeout-decorator>=0.5.0
regex>=2023.0.0
multiprocess>=0.70.0

# Optional API/eval clients
openai>=1.5.0
